{
  "name": "forge-rovo-partner-portal",
  "version": "0.0.0",
  "private": true,
  "description": "A Rovo agent for searching Partner Portal content using Confluence CQL",
  "license": "Apache-2.0",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "changelog": "git cliff",
    "check": "biome check --write",
    "clean": "rm -rf ./dist",
    "eval": "promptfoo eval",
    "format": "biome format --write",
    "lint": "biome lint",
    "test": "vitest",
    "types": "actiontypes && biome format --write && tsc",
    "view": "promptfoo view"
  },
  "dependencies": {
    "@forge/api": "^7.0",
    "neverthrow": "^8.2"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4",
    "@types/node": "^25.3",
    "promptfoo": "^0.120",
    "tsx": "^4.21",
    "typescript": "^5.9",
    "vitest": "^4.0",
    "yaml": "^2.8"
  }
}
