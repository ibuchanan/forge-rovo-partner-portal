{
  "name": "explore-forge-wretch",
  "version": "0.0.0",
  "description": "a sample forge app with rovo-agent module",
  "main": "index.ts",
  "private": true,
  "license": "Apache-2.0",
  "dependencies": {
    "@forge/api": "^7.0",
    "neverthrow": "^8.2"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4",
    "@types/node": "^25.3",
    "promptfoo": "^0.120",
    "tsx": "^4.21",
    "typescript": "^5.9",
    "vitest": "^4.0",
    "yaml": "^2.8"
  },
  "scripts": {
    "check": "biome check --write",
    "format": "biome format --write",
    "lint": "biome lint",
    "changelog": "git cliff",
    "eval": "promptfoo eval",
    "view": "promptfoo view",
    "build": "tsc",
    "clean": "rm -rf ./dist",
    "test": "vitest",
    "types": "actiontypes && biome format --write && tsc"
  }
}
